# First signed archive and App Store Connect upload — 2026-08-13

## Outcome

The first signed device archive of AuraFit was produced and uploaded to
App Store Connect. `Upload succeeded` / `** EXPORT SUCCEEDED **` at
2026-08-13 20:41 local.

## What this evidences

- `AURA-OPS-012A` (signed archive): archive succeeded via
  `xcodebuild archive -destination "generic/platform=iOS"
  -allowProvisioningUpdates CODE_SIGN_STYLE=Automatic
  DEVELOPMENT_TEAM=796XH483R4`; `codesign -dv` reports
  `com.pchordia.aurafit`, team `796XH483R4`.
- `AURA-OPS-009` (account/agreements): the account session permitted
  provisioning and upload, so membership and agreements were functional at
  upload time.
- `AURA-OPS-010` (App Store Connect record): the upload was accepted, so a
  record for `com.pchordia.aurafit` exists.
- Source: branch `claude/phase0-production-readiness` at `d8dccf2`;
  `MARKETING_VERSION 1.0`, `CURRENT_PROJECT_VERSION 1`.
- Archive retained at `/private/tmp/aurafit-build/AuraFit-1.0-1.xcarchive`.

## Not evidenced by this upload

- Apple processing completion and TestFlight availability of build 1.0 (1)
  (verify in App Store Connect).
- Physical-device install/run (`AURA-OPS-012B`, `AURA-QA-002/004/005`).
- Export-compliance determination review (`AURA-OPS-014`) — the target sets
  `ITSAppUsesNonExemptEncryption = NO`, which the upload accepted; owner
  review of that answer remains open.
- Owner approvals for TestFlight copy, StoreKit catalog, and legal items.
- Build number 1 is now consumed; `AURA-OPS-011` should freeze identity and
  bump `CURRENT_PROJECT_VERSION` to 2 for the next upload.
