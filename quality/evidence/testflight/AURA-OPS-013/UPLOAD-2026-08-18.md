# AURA-OPS-013 — build `1.0 (3)` upload and processing — 2026-08-18

- Status after this record: `done` for build 3 (upload succeeded, Apple processed the build, it
  was attached to version 1.0 and submitted). Full context:
  `quality/evidence/release/1.0-3-full-free/SUBMISSION-2026-08-18.md`.
- Operator: owner's assistant on the owner's Mac, owner-approved. Upload path used:
  `xcodebuild -exportArchive` with `method app-store-connect`, `destination upload`,
  `-allowProvisioningUpdates` (not Organizer; same signed archive as `AURA-OPS-012B`).

```text
archive path / SHA / frozen tuple / uploader role:
  /private/tmp/AuraFit-1.0-3-export/AuraFit-1.0-3.xcarchive
  local-export IPA SHA-256 d5300abd3b31a1b1d11692d0d55b072a28736625d052efef8445fbf02e606664
  com.pchordia.aurafit 1.0 (3), team 796XH483R4; owner's developer account session (role not recorded)
delivery ID / upload time / processing first-seen-final state:
  delivery ID not captured; "Upload succeeded" + "** EXPORT SUCCEEDED **" at 2026-08-18 13:34:57 America/New_York;
  processed within ~6 minutes — build 3 selectable in the version 1.0 build picker and attached
Apple message/code / classification / next action:
  none observed (no Invalid Binary, no Missing Compliance prompt); next action = App Review outcome
metadata comparisons (bundle, tuple, OS, variants, privacy, symbols, compliance):
  bundle match; tuple match; min OS 18.0 (from IPA inspection, README.md); device variants unavailable (not read from ASC);
  privacy manifest match (IPA inspection); symbols uploadSymbols=true (export options), ASC dSYM state unavailable;
  compliance ITSAppUsesNonExemptEncryption=NO carried in bundle, no prompt
consumed tuple and next-build rule:
  1.0 (3) consumed; builds 1 and 2 remain in ASC unused; next build ≥ 4
blocker:
  none for upload/processing; App Review pending
```

Subtasks: ST-01 done (via `xcodebuild` instead of Organizer — owner-approved path), ST-02 partial
(delivery ID not captured), ST-03 done (processed; timestamps approximate), ST-04 done (no
message), ST-05 partial (items marked `unavailable` above were not read from ASC), ST-06 done.
